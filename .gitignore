node_modules
dist
dist-template
.vite
*.log
.env
.env.local
.DS_Store
coverage
playwright-report
test-results
.tsbuildinfo
*.tsbuildinfo

# Local agent tooling (not project files)
mcps
terminals

# Native add-in build output
native/build

# Rasterized/recorded artifacts (regenerable from SVG / the app)
spec/visual/*.png

# overlay harness capture artifact (written to CWD by ppoverlay.exe)
overlay.png

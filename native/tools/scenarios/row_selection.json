{
  "name": "row_selection",
  "description": "S3 row-selection UX gate. Exercises the overlay harness end-to-end incl the ROWSEL stage (rail selection, row app-bar ops, delete cascade, PP_ROWY band coverage). Produces artifacts for visual review.",
  "exe": "ppoverlay",
  "args": [],
  "timeout": 540,
  "retries": 1,
  "expected_markers": ["OWNSEL PASS", "SCOPE PASS", "ROWSEL PASS", "INPUT NEUTRAL OK"]
}
